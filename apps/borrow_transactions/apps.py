from django.apps import AppConfig


class BorrowTransactionsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.borrow_transactions"
    label = "borrow_transactions"